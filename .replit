modules = ["python-3.11"]

[nix]
channel = "stable-24_05"
packages = ["libxcrypt", "postgresql"]

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "NeuraLex Platform Server"

[[workflows.workflow]]
name = "NeuraLex Platform Server"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "pip install -r requirements.txt && python -m uvicorn app.main:app --host 0.0.0.0 --port 5000"
waitForPort = 5000

[deployment]
run = ["sh", "-c", "pip install -r requirements.txt && python -m uvicorn app.main:app --host 0.0.0.0 --port 5000"]
